If you are not currently working on an active step, use 3 sub-agents to create a council.
Use `model = "gpt-5.3-codex-spark"` for the sub-agents.
Use `model_reasoning_effort = "xhigh"` for the sub-agents.
The sub-agents may search the web for information to help them.
The sub-agents may search and reason about improvements and design patterns as part of their suggestion.
Have the council determine what is needed to improve this set of tasks.
Have the council present their findings.
Then have the council vote on the best result.
If the result is a tie, break the tie yourself.
Then end or kill the sub-agents.
Then perform the best result.
